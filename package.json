{
  "name": "data-lake",
  "version": "0.1.0",
  "bin": {
    "data-lake": "bin/data-lake.js"
  },
  "scripts": {
    "build": "echo \"The build step is not required when using JavaScript!\" && exit 0",
    "cdk": "cdk",
    "test": "jest"
  },
  "devDependencies": {
    "aws-cdk": "2.14.0",
    "jest": "^26.4.2"
  },
  "dependencies": {
    "aws-cdk-lib": "2.14.0",
    "constructs": "^10.0.0"
  }
}
